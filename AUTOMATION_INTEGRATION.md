# ربط النشر الآلي

بعد توليد التقرير الكامل تحدّث وظيفة `competitors-report` مستند Firestore: المجموعة `reports`، المستند `competitor_latest`، والحقلان `rawMD` و`updatedAt`.

داخل كل منشور يضيف المولد:

```md
**السعر أو العرض المستخرج:** اسم المنتج والسعر أو نسبة الخصم
```

مصدر الحقل هو التفريغ الصوتي فقط. يكتب `N/A` عند غياب سعر رقمي بعملة أو نسبة خصم رقمية. الموقع يملك كاشفًا احتياطيًا للتقارير القديمة.
